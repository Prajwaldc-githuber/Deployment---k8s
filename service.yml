apiVersion: v1
kind: Service
metadata:
  name: stateless-service
spec:
  type: NodePort
  selector:
    app: stateless-app
  ports:
    - port: 80          # Service Port
      targetPort: 80    # Pod Container Port
      nodePort: 30080   # Accessible from outside (NodeIP:30080)
